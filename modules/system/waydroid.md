# Initialize with Google Play Services (GAPPS) or vanilla (VANILLA)
sudo waydroid init -s GAPPS

# Start the container
systemctl start waydroid-container

# Launch MT5 / Android UI
waydroid show-full-ui
